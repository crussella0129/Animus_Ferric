# Copy to `.env` to override. The compose default already points at the shared
# Animus model store (../../Animus/Models), so most setups need nothing here.
#
# Set this only if your GGUFs live somewhere else — a NAS share, another drive:
#MODELS_PATH=Y:\Models\gguf
