# Minimal Squid configuration for an allowlist proxy
# Deny all by default, only allow specific domains

acl localnet src 10.0.0.0/8
acl localnet src 172.16.0.0/12
acl localnet src 192.168.0.0/16
acl localnet src fc00::/7
acl localnet src fe80::/10

acl SSL_ports port 443
acl Safe_ports port 80          # http
acl Safe_ports port 443         # https
acl CONNECT method CONNECT

# Allowlist definition
acl allowed_domains dstdomain .example.com
acl allowed_domains dstdomain .wikipedia.org

http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports

# Only allow requests to the allowed domains from the local network
http_access allow localnet allowed_domains
http_access deny all

http_port 3128
