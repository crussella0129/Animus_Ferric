# Sprint 59: Implement the `shell_exec` Tool
**Status**: in-progress
**Goal**: Build the `shell_exec` tool (Ring 2) with workspace cwd restriction, command timeout, stdout/stderr capture, output caps, and Ornstein command screening for destructive/exfil/privesc patterns (including ADR-045 permission model extension).
