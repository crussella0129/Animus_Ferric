# Sprint 0 E2E Tests

Not yet possible — unlocked by sprint s1 (first real Provider backend + ported L0–L6 benchmark harness + Llama-3.2-1B as cheapest fleet model).

First E2E: **L0 smoke — one real-GGUF run produces a valid trace and a correct file edit.** CPU-only, therefore hardware-independent. `mock_loop_skeleton` (see integration-tests.md) is the structural template.
