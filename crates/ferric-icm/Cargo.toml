[package]
name = "ferric-icm"
description = "Interpretable Context Methodology (ICM): filesystem-as-agent-architecture — numbered stage folders + CONTEXT.md contracts scope what each stage-agent sees; the folder structure IS the delegation logic (ADR-064)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ferric-guard = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
